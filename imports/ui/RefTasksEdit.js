import React from "react";
import { Flex, Text } from "theme-ui";

const RefTasksEdit = (props) => (
  <Flex sx={{ variant: "flex.ref" }}>
    <Text>Tasks Edit</Text>
  </Flex>
);

export default RefTasksEdit;
