import React from "react";
import { Flex, Text } from "theme-ui";

const RefResourceView = (props) => (
  <Flex sx={{ variant: "flex.ref" }}>
    <Text>Resource view</Text>
  </Flex>
);

export default RefResourceView;
