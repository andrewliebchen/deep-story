import React from "react";
import { Box, Flex, Text } from "theme-ui";

const RefResourceEdit = (props) => (
  <Box>
    <Flex sx={{ variant: "flex.ref" }}>
      <Text>Resource edit</Text>
    </Flex>
  </Box>
);

export default RefResourceEdit;
